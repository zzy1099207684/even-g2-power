# No build-time configuration. The relay URL, Deepgram key, and translation
# model settings are all entered by the user in the app's Settings screen and
# stored via the bridge — nothing is baked into the bundle.
