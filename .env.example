# Cloudflare Worker relay base URL (no trailing slash). The relay holds
# DEEPSEEK_API_KEY and DEEPGRAM_API_KEY server-side — this app never sees either key.
VITE_RELAY_URL=https://g2-translate-relay.zzy1099207684.workers.dev
